export const DEMO_PHONE = "";

/** Indian mobile: exactly this many digits for valid phone + green check on login. */
export const MIN_PHONE_DIGITS = 10;

/** Minimum password length when using password login. */
export const MIN_LOGIN_PASSWORD_LENGTH = 6;

export { OTP_LEN } from "./otp";
